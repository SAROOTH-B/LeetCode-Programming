class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector <int> vector1;
        for(int i=0;i<nums.size();i++)
            vector1.push_back(nums[nums[i]]);
        return vector1;
    }
};
